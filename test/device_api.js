var superagent = require('superagent');
var expect = require('expect.js');

describe('express device api', function() {
  it('tests positive', function() {
    expect(1).to.eql(1);

  });
  it('fails test', function() {
    expect(true).to.eql(false);
  });
});
